<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlowFinance C6 - Student Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet">
    <style>
        body { 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
        }
        .glass { 
            background: rgba(255, 255, 255, 0.7); 
            backdrop-filter: blur(12px); 
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
    </style>
</head>
<body class="antialiased">

    <div class="max-w-6xl mx-auto px-4 py-8">
        <header class="flex flex-col md:flex-row justify-between items-start md:items-center mb-10 gap-6">
            <div>
                <div class="flex items-center gap-2 mb-1">
                    <div class="bg-indigo-600 p-2 rounded-xl shadow-lg shadow-indigo-200">
                        <span class="material-icons-round text-white">auto_graph</span>
                    </div>
                    <h1 class="text-3xl font-extrabold text-slate-900 tracking-tight">FlowFinance <span class="text-indigo-600">C6</span></h1>
                </div>
                <p class="text-slate-500 font-medium">Hai Mahasiswa! Pantau uang saku kamu di sini.</p>
            </div>
            
            <div class="flex items-center gap-4">
                <div class="hidden md:block text-right">
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-widest">Server Port</p>
                    <p class="text-sm font-bold text-indigo-600">2583 (Nginx Active)</p>
                </div>
                <a href="/add" class="flex items-center bg-slate-900 hover:bg-indigo-600 text-white px-6 py-3.5 rounded-2xl font-bold transition-all shadow-xl hover:scale-105 active:scale-95">
                    <span class="material-icons-round mr-2">add_circle</span>
                    Catat Transaksi
                </a>
            </div>
        </header>

        <% 
            let income = transactions.filter(t => t.type === 'Income').reduce((a, b) => a + Number(b.amount), 0);
            let expense = transactions.filter(t => t.type === 'Expense').reduce((a, b) => a + Number(b.amount), 0);
            let balance = income - expense;
            let statusColor = balance < 0 ? 'text-red-500' : 'text-emerald-500';
        %>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
            <div class="bg-gradient-to-br from-indigo-600 to-violet-700 p-8 rounded-[2.5rem] shadow-2xl shadow-indigo-200 relative overflow-hidden group">
                <div class="relative z-10">
                    <p class="text-indigo-100 font-semibold text-sm mb-2 opacity-80">Sisa Saldo Kamu</p>
                    <h2 class="text-4xl font-extrabold text-white">Rp <%= balance.toLocaleString('id-ID') %></h2>
                    <div class="mt-6 flex items-center gap-2 bg-white/10 w-fit px-3 py-1 rounded-full border border-white/20">
                        <span class="text-[10px] font-bold text-white uppercase tracking-tighter">Budgeting Status: Aman</span>
                    </div>
                </div>
                <span class="material-icons-round absolute -right-6 -bottom-6 text-white/10 text-[12rem] group-hover:scale-110 transition-transform duration-500 text-nowrap">wallet</span>
            </div>

            <div class="glass p-8 rounded-[2.5rem] shadow-sm flex flex-col justify-between">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-emerald-100 text-emerald-600 p-3 rounded-2xl">
                        <span class="material-icons-round">south_west</span>
                    </div>
                    <span class="text-[10px] font-bold text-emerald-500 bg-emerald-50 px-2 py-1 rounded">INCOME</span>
                </div>
                <div>
                    <p class="text-slate-400 text-xs font-bold uppercase tracking-widest mb-1">Pemasukan</p>
                    <h3 class="text-2xl font-bold text-slate-800">Rp <%= income.toLocaleString('id-ID') %></h3>
                </div>
            </div>

            <div class="glass p-8 rounded-[2.5rem] shadow-sm flex flex-col justify-between">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-rose-100 text-rose-600 p-3 rounded-2xl">
                        <span class="material-icons-round">north_east</span>
                    </div>
                    <span class="text-[10px] font-bold text-rose-500 bg-rose-50 px-2 py-1 rounded">EXPENSE</span>
                </div>
                <div>
                    <p class="text-slate-400 text-xs font-bold uppercase tracking-widest mb-1">Pengeluaran</p>
                    <h3 class="text-2xl font-bold text-slate-800">Rp <%= expense.toLocaleString('id-ID') %></h3>
                </div>
            </div>
        </div>

        <div class="glass rounded-[2.5rem] shadow-xl overflow-hidden mb-12">
            <div class="px-8 py-8 border-b border-slate-200/50 flex justify-between items-center bg-white/30 text-nowrap">
                <h3 class="text-xl font-extrabold text-slate-800 tracking-tight">Riwayat Jajan & Nabung</h3>
                <div class="text-xs font-bold text-slate-500 bg-slate-200/50 px-4 py-2 rounded-xl">
                    Total: <%= transactions.length %> Data
                </div>
            </div>
            
            <div class="overflow-x-auto px-4 pb-4">
                <table class="w-full text-left border-separate border-spacing-y-3">
                    <thead>
                        <tr class="text-slate-400 text-[10px] uppercase font-bold tracking-[0.2em]">
                            <th class="px-6 py-4">Informasi Transaksi</th>
                            <th class="px-6 py-4">Kategori</th>
                            <th class="px-6 py-4">Nominal</th>
                            <th class="px-6 py-4 text-center">Tindakan</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% transactions.forEach(t => { %>
                        <tr class="bg-white hover:bg-indigo-50/50 transition-all duration-300 shadow-sm group">
                            <td class="px-6 py-5 rounded-l-[1.5rem]">
                                <div class="font-bold text-slate-700 group-hover:text-indigo-700 transition-colors"><%= t.description %></div>
                                <div class="text-[10px] text-slate-400 font-medium"><%= new Date(t.created_at).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %></div>
                            </td>
                            <td class="px-6 py-5">
                                <span class="bg-indigo-50 text-indigo-600 px-4 py-1.5 rounded-full text-[10px] font-bold uppercase tracking-wider">
                                    <%= t.category_name %>
                                </span>
                            </td>
                            <td class="px-6 py-5 font-black text-base <%= t.type === 'Income' ? 'text-emerald-500' : 'text-rose-500' %>">
                                <%= t.type === 'Income' ? '+' : '-' %> Rp <%= Number(t.amount).toLocaleString('id-ID') %>
                            </td>
                            <td class="px-6 py-5 rounded-r-[1.5rem] text-center">
                                <div class="flex justify-center gap-2">
                                    <a href="/edit/<%= t.id %>" class="w-10 h-10 flex items-center justify-center bg-slate-100 text-slate-400 rounded-xl hover:bg-indigo-600 hover:text-white transition-all shadow-sm">
                                        <span class="material-icons-round text-sm">edit</span>
                                    </a>
                                    <a href="/delete/<%= t.id %>" onclick="return confirm('Yakin mau hapus catatan ini?')" class="w-10 h-10 flex items-center justify-center bg-slate-100 text-slate-400 rounded-xl hover:bg-rose-500 hover:text-white transition-all shadow-sm text-nowrap">
                                        <span class="material-icons-round text-sm">delete</span>
                                    </a>
                                </div>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <footer class="text-center pb-8">
            <p class="text-slate-400 text-[10px] font-bold uppercase tracking-[0.3em]">&copy; 2026 Kelompok C6 - Solusi Keuangan Mahasiswa</p>
        </footer>
    </div>

</body>
</html>
